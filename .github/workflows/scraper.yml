name: Playwright Table Scraper

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  scrape-tables:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install playwright
        playwright install chromium
        playwright install-deps
    
    - name: Run scraper for 23f3000704@ds.study.iitm.ac.in
      run: |
        python scraper.py
```

### 3. Commit and push to GitHub

### 4. Create a GitHub Personal Access Token:
1. Go to GitHub Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token (classic)
3. Select scopes: `repo` (full control) and `workflow`
4. Generate and copy the token

### 5. Trigger the workflow:
- Either push a commit, or
- Go to Actions tab → Select the workflow → Click "Run workflow"

### 6. Format your answer:
```
https://github.com/YOUR_USERNAME/YOUR_REPO ghp_YOUR_TOKEN_HERE
